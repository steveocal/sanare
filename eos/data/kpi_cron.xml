<odoo>
    <record id="ir_cron_eos_kpi_snapshot" model="ir.cron">
        <field name="name">EOS: snapshot KPIs at week/month end</field>
        <field name="model_id" ref="model_eos_kpi_value"/>
        <field name="state">code</field>
        <field name="code">model._cron_snapshot()</field>
        <field name="interval_number">1</field>
        <field name="interval_type">days</field>
        <field name="nextcall" eval="(DateTime.now().replace(hour=23, minute=30, second=0, microsecond=0)).strftime('%Y-%m-%d %H:%M:%S')"/>
        <field name="active" eval="True"/>
    </record>
</odoo>
